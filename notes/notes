session 10 has full autoscaler
project for this class has full writeup on sql and secrets


session 11 has full notes on network policy.
Note: once you restrict access based on selectors, you will block all others.

register with AWS to get access to t3.small - T3.micro isn't working well system is unstable

kubectl run -i --tty load-generator --image=busybox /bin/sh

while true; do wget -q -O- http://php-apache.default.svc.cluster.local; done
